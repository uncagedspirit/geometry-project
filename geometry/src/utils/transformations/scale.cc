//dummy code for testing change later


#include "transformer.h"
#include <iostream>

std::vector<double> Transformer::scale(std::vector<double> params, double factor) {
    std::cout << "[Dummy] Scaling with factor: " << factor << "\n";
    return params;
}
